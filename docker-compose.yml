services:
  xterm-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8787:8787"
    environment:
      - TERM=xterm-256color
    volumes:
      - ./:/workspace
    stdin_open: true
    tty: true
