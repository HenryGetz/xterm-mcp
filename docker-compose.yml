services:
  xterm-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8787:8787"
    environment:
      - TERM=xterm-256color
    volumes:
      - ./:/workspace
    stdin_open: true
    tty: true

